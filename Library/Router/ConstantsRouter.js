import { DRIVEDIRECTLINK } from "../Constants/DriveDirectLink/DriveDirectLink.js";

const CONSTANTSROUTER=()=>{
const DATA=`const DRIVEDIRECTLINK='${DRIVEDIRECTLINK}';
`;
localStorage.setItem("CONSTANTS",DATA);
};
export{CONSTANTSROUTER};
`
const DRIVEDIRECTLINK='${DRIVEDIRECTLINK}';
`