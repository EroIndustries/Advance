import { ADD } from "../Functions/Add/Add.js"; 
import { APPMODE } from "../Functions/AppMode/AppMode.js"; 
import { APPUPDATER } from "../Module/UpdateModule.js"; 
import { BODY } from "../Functions/Body/Body.js"; 
import { BREAK } from "../Functions/Break/Break.js"; 
import { CALL } from "../Functions/Call/Call.js"; 
import { CHECKER } from "../Functions/Checker/Checker.js"; 
import { CLEAR } from "../Functions/Clear/Clear.js"; 
import { CONDITION } from "../Functions/Condition/Condition.js"; 
import { CREATEELEMENT } from "../Functions/CreateElement/CreateElement.js"; 
import { DATENOW } from "../Functions/Datenow/Datenow.js"; 
import { DECLARATION } from "../Functions/Declaration/Declaration.js"; 
import { DEJSON } from "../Functions/Dejson/Dejson.js"; 
import { DEVICED } from "../Functions/Device/Device.js"; 
import { DISPLAY } from "../Functions/Display/Display.js"; 
import { ENVIRONMENT } from "../Functions/Environment/Environment.js"; 
import { ENVIRONMENTCHECKER } from "../Functions/EnvironmentChecker/EnvironmentChecker.js"; 
import { EVENT } from "../Functions/Event/Event.js"; 
import { FACEBOOK } from "../Functions/Facebook/Facebook.js"; 
import { FINDER } from "../Functions/Finder/Finder.js"; 
import { HIDER } from "../Functions/Hider/Hider.js"; 
import { MODULER } from "../Functions/Module/Module.js"; 
import { NAVIGATOR } from "../Functions/Navigator/Navigator.js"; 
import { PASSWORDDEHASH } from "../Functions/PasswordDeharsh/PasswordDeharsh.js"; 
import { PASSWORDHASH } from "../Functions/PasswordHarsh/PasswordHarsh.js"; 
import { RANDOMCODE } from "../Functions/RandomCode/RandomCode.js"; 
import { REDUX } from "../Functions/Redux/Redux.js"; 
import { RELOAD } from "../Functions/Reload/Reload.js"; 
import { STORE } from "../Functions/Store/Store.js"; 
import { STYLED } from "../Functions/Style/Style.js"; 
import { TIMENOW } from "../Functions/TimeNow/TimeNow.js"; 
import { WEBSITE } from "../Functions/Website/Website.js"; 
import { APPCOLOR } from "../Functions/AppColor/App.js";

const FUNCTIONSROUTE = () => { 
    const FUNCTIONS = `
        const ADD = ${ADD.toString()}; 
        const APPMODE = ${APPMODE.toString()}; 
        const APPCOLOR=${APPCOLOR.toString()};
        const APPUPDATER = ${APPUPDATER.toString()}; 
        const BODY = ${BODY.toString()}; 
        const BREAK = ${BREAK.toString()}; 
        const CALL = ${CALL.toString()}; 
        const CHECKER = ${CHECKER.toString()}; 
        const CLEAR = ${CLEAR.toString()}; 
        const CONDITION = ${CONDITION.toString()}; 
        const CREATEELEMENT = ${CREATEELEMENT.toString()}; 
        const DATENOW = ${DATENOW.toString()}; 
        const DECLARATION = ${DECLARATION.toString()}; 
        const DEJSON = ${DEJSON.toString()}; 
        const DEVICED = ${DEVICED.toString()}; 
        const DISPLAY = ${DISPLAY.toString()}; 
        const ENVIRONMENT = ${ENVIRONMENT.toString()}; 
        const ENVIRONMENTCHECKER = ${ENVIRONMENTCHECKER.toString()}; 
        const EVENT = ${EVENT.toString()}; 
        const FACEBOOK = ${FACEBOOK.toString()}; 
        const FINDER = ${FINDER.toString()}; 
        const HIDER = ${HIDER.toString()}; 
        const MODULER = ${MODULER.toString()}; 
        const NAVIGATOR = ${NAVIGATOR.toString()}; 
        const PASSWORDDEHASH = ${PASSWORDDEHASH.toString()}; 
        const PASSWORDHASH = ${PASSWORDHASH.toString()}; 
        const RANDOMCODE = ${RANDOMCODE.toString()}; 
        const REDUX = ${REDUX.toString()}; 
        const RELOAD = ${RELOAD.toString()}; 
        const STORE = ${STORE.toString()}; 
        const STYLED = ${STYLED.toString()}; 
        const TIMENOW = ${TIMENOW.toString()}; 
        const TEXTCOLOR = ${TEXTCOLOR.toString()}; 
        const WEBSITE = ${WEBSITE.toString()}; 
    `;
    localStorage.setItem("FUNCTIONS", FUNCTIONS);  
};

export { FUNCTIONSROUTE };
